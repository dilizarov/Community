<% content_for :title do %>
  <%= "Password Reset" %>
<% end %>

<%= react_component 'ResetPassword', { success: @success, email: @user && @user.email, username: @user && @user.username, avatar: @user && @user.avatar.url, reset_token: @user && @user.reset_password_token } %>
