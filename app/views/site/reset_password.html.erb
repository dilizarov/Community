<% content_for :title do %>
  <%= "Password Reset" %>
<% end %>

<%= react_component 'ResetPassword', { success: @success, email: @user.email, username: @user.username, avatar: @user.avatar.url, reset_token: @user.reset_password_token } %>
